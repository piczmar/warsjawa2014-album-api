import grails.rest.render.json.*
import album.*

beans = {
	
	/*
	 commentCollectionRenderer(JsonCollectionRenderer, Comment) {
		 excludes = ['class']
	 }
  
	 commentRenderer(JsonRenderer, Comment) {
		 excludes = ['class']
	 }
	 
	 pictureRenderer(JsonRenderer, Picture) {
		 excludes = ['class', 'dateCreated']
	 }
	 
	 pictureCollectionRenderer(JsonCollectionRenderer, Picture) {
		 excludes = ['class']
	 }
	 */
}
